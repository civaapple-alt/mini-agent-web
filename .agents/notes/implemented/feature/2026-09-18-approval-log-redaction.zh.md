# 审批日志脱敏

状态：Implemented

审批日志位于 SDK 和 Gateway 的边界，不能直接复制工具的自由文本参数。现在日志只记录：

- 审批阶段；
- 工具名；
- `apply_patch` 的新增、修改、删除计数；
- 请求 ID。

Shell 命令、文件路径和其他完整 `actionSummary` 不写入日志。前端审批事件仍保留原有结构化数据，
因此不影响审批展示、授权匹配或工具执行。

验证覆盖 Shell 命令不出现在 SDK 审批日志，以及 `apply_patch` 计数的有界投影。
